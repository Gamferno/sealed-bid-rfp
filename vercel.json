{
  "buildCommand": "cd contracts && npm run build && cd ../ui && npm run build",
  "outputDirectory": "ui/dist",
  "installCommand": "npm install --legacy-peer-deps || npm install",
  "framework": null
}
